# Path to the Bitcoin Education Content repository
BEC_REPO=/path/to/bitcoin_education_content

# YouTube OAuth2 credentials
# Download your OAuth2 client secrets JSON file from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
YOUTUBE_CLIENT_SECRETS_FILE=/path/to/client_secrets.json

# PeerTube instance credentials
# Format: https://your-peertube-instance.com (without trailing slash)
PEERTUBE_INSTANCE=https://peertube.example.com
PEERTUBE_USERNAME=your_username
PEERTUBE_PASSWORD=your_password

# Optional: Use separate upload endpoint to bypass proxy (e.g., https://upload.peertube.example.com)
# Leave empty to use PEERTUBE_INSTANCE for uploads
PEERTUBE_UPLOAD_ENDPOINT=

# Optional: Disable SSL verification for upload endpoint (set to false if SSL cert fails)
# Default: true
PEERTUBE_VERIFY_SSL=false

# Optional: Automatically set thumbnail at 4 seconds for new PeerTube uploads
# Default: true (can be disabled if you want to use PeerTube's auto-generated thumbnails)
PEERTUBE_AUTO_THUMBNAIL=true